# Backend Environment Configuration
# Copy this file to .env and fill in your values

# Server
PORT=4000
NODE_ENV=development

# The backend does not store FortiGate credentials in env.
# Firewalls are added via the dashboard UI and stored in SQLite.
